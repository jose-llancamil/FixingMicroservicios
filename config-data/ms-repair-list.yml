server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/msRepairListDB
    username: postgres
    password: admin
eureka:
  instance:
    hostname: localhost
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka