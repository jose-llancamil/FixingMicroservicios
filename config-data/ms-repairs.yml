server:
  port: 8091
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/msRepairDB
    username: postgres
    password: admin
eureka:
  instance:
    hostname: localhost
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka